# our_table - Sistem Reservasi Restoran
## Setup
1. Install dependencies: `npm install`
2. Create database `our_table` in MySQL
3. Create folder `uploads` in project root
4. Run: `npm start` (server on port 3000)
